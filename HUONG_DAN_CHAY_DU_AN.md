mở cmd (windows + R, gõ cmd)

vào nơi chứa dự án SellBookWeb

FRONT-END:
cd .../SellBookWeb-frontend

chạy npx http-server -p 8080


BACK-END:
cd .../SellBookWeb-backend

chạy mvn spring-boot:run


khi chạy xong thì mở trình duyệt và truy cập vào http://localhost:8080/login để đăng nhập.